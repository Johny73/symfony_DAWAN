{% extends 'base.html.twig' %}

{% block body %}
    <h1>Liste des jeux de société</h1>
    <ul>
        {%  for board_game in board_games %}
            <li><a href="{{ path('app_boardgame_show', { id: board_game.id }) }}"> {{ board_game.name }}</a></li>
        {% endfor %}
    </ul>
{% endblock %}

